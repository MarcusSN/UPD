# УПД Конвертер - Electron Приложение

Конвертер УПД из Excel в XML для электронного документооборота (СБИС, Контур.Диадок и др.)

**Формат XML:** ON_NSCHFDOPPR версия 5.03

## 📥 Скачать портативную версию

После каждого коммита в ветку `main` или `claude/**` автоматически собирается портативная версия для Windows.

### Способ 1: Скачать из GitHub Actions Artifacts

1. Перейдите на вкладку [**Actions**](../../actions)
2. Выберите последний успешный workflow **"Build and Release"**
3. Скачайте артефакт **UPD-Converter-Portable** (доступен 90 дней)
4. Распакуйте ZIP архив и запустите `UPD-Converter-Portable.exe`

### Способ 2: Создать релиз с тегом

Создайте тег версии и GitHub автоматически создаст релиз:

```bash
git tag v1.0.0
git push origin v1.0.0
```

После этого portable файл будет доступен на странице [**Releases**](../../releases)

## 🚀 Возможности

- ✅ Drag & Drop файлов
- ✅ Пакетная обработка (50+ файлов)
- ✅ Мультиязычный интерфейс (🇷🇺 RU / 🇬🇧 EN / 🇨🇳 ZH)
- ✅ Современный интерфейс в стиле Anthropic
- ✅ Настраиваемый маппинг колонок Excel
- ✅ Превью XML перед сохранением
- ✅ Горячие клавиши (Ctrl+O, Enter, Escape)

## 🛠️ Разработка

### Требования

- Node.js 18+
- npm

### Установка зависимостей

```bash
cd upd-converter-app
npm install
```

### Запуск в режиме разработки

```bash
npm start
```

### Сборка

**Локально (на Windows):**
```bash
npm run build:portable
```

**Через GitHub Actions (рекомендуется):**
- Просто сделайте push в ветку `main` или `claude/**`
- GitHub автоматически соберет portable версию
- Скачайте из раздела Actions → Artifacts

## 📦 Структура проекта

```
.
├── .github/
│   └── workflows/
│       └── build-release.yml    # Автоматическая сборка на GitHub
├── upd-converter-app/           # Electron приложение
│   ├── package.json             # Зависимости
│   ├── main.js                  # Главный процесс
│   ├── renderer.js              # UI логика
│   ├── converter.js             # Конвертация Excel → XML
│   ├── locales.js               # Переводы (RU/EN/ZH)
│   ├── index.html               # Интерфейс
│   └── styles.css               # Стили
└── README.md
```

## ⚠️ Windows SmartScreen

При первом запуске Windows может показать предупреждение SmartScreen:

1. Нажмите **"Подробнее"** (More info)
2. Нажмите **"Выполнить в любом случае"** (Run anyway)

**Почему?** Приложение не имеет платной цифровой подписи ($200-400/год). Это НЕ вирус!

## 📖 Использование

1. **Перетащите Excel файлы** в окно программы
2. **Выберите папку** для сохранения XML
3. **Нажмите "Конвертировать"**
4. Готово! XML файлы появятся в выбранной папке

### Смена языка

Нажмите на флаг в правом верхнем углу для выбора языка.

### Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| `Ctrl+O` | Открыть файлы |
| `Enter` | Конвертировать |
| `Escape` | Закрыть настройки |

## 📝 Лицензия

MIT - свободное использование
